package org.firstinspires.ftc.teamcode.enums;


// An enum that is used by moveDirection method to represent one of four directions that the robot can move in
public enum Direction {
    FORWARD, BACKWARD, LEFT, RIGHT
}
